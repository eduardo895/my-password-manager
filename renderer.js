document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('openVault');
    btn.addEventListener('click', () => {
        alert("Ainda por implementar!")
    });
})